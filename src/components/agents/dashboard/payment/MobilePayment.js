// @ts-nocheck
import React from "react";

const MobilePayment = (name) => {
  return (
    <div>
      <p className="fw-bold">Mobile payment is not supported, currently!</p>
    </div>
  );
};

export default MobilePayment;
